# Forge Autopilot State

Last completed product task: P4-04 · 配置生成表单 (DONE on macOS arm64; real Host inspection and restricted UI form verified)
Paused product task: None
Last completed migration task: MIG-PY-09 · Node Host Retirement & P2 Revalidation (DONE on macOS arm64 development runtime; installer/platform risks deferred)
Current task: P4-05 · Claude SDK真实适配 (`BLOCKED`; official API key absent, user deferred live acceptance)
Next candidate: P4-06 (only after P4-05 passes)
Current track: canonical product Autopilot at an explicit credential/cost Hard Stop; no independent executable task remains in the authoritative dependency chain
Blocked tasks: P4-05 (requires authorized Anthropic API key and bounded paid live fixture); P4-06 onward depend on it
Migration authority: Explicit user decision and `docs/decisions/0029-python-core-runtime-architecture.md`; implementation order in `docs/forge-python-core-migration-plan.md`. Product Task/Test IDs in the read-only spec remain unchanged.
Deferred verification: `docs/deferred-verification.json` preserves cross-phase Test IDs. No deferred case is PASSED. P4-01 T036/T037 gained P4-02 evidence; P4-01/P4-02 T038 gained P4-03 evidence. T040 from P4-01～03 remains mapped to P4-09. P4-04 T106–T110 remain mapped to P6-01/P6-02/P6-07. P3-12 T116–T120 remain mapped to P6/P9. 37 deferred entries remain.
Phase gate: P2 and P3 exits passed on macOS arm64 Python-only Desktop development path. P3 used version-bound CodeSnapshot, real Codex development, approved Verify, structured Review, separate human acceptance and explicit local merge, with independent failure/waiver/crash fixtures. See `docs/p3-completion-report.md` and `docs/demo/p3-delivery.md`. P4-05 is blocked on an explicitly unavailable paid API credential; P4 Phase Gate has not passed.
P4-05 partial work: Official Python Claude Agent SDK 0.2.159 pinned with frozen uv lock and license inventory. A real owned offline `claude --version` process loaded the bundled CLI 2.1.281 on macOS arm64; API key presence false and `liveVerified=false`. No Claude plugin registered, no cloud call, no fake Executor capability. User said they have no Claude API and to omit this live acceptance for now. T041–T045 and canonical P4-05 acceptance remain unpassed; P4-06 through the subsequent dependency chain cannot start. Frozen pnpm install, contracts 47/0 errors/4 old warnings, task map 37 deferred, Python 111 pytest/Ruff/strict mypy 45 source files, lint/typecheck/test/build, real Electron Desktop smoke/schema24 and diff check all exit 0. See ADR 0052 and implementation/compatibility records.
P4-04: Python Host fixed `plugin.inspectBundled` reads only the lock-checked Codex bundle and returns real version/API/compatibility/active state plus closed Schema. Main/Preload/Client expose one read-only, sender-checked method. `@forge/ui` form creates scalar fields from that Schema, strips unknown keys, rejects raw secret input and accepts only masked `credential:<id>` references. Actual bundled Codex Schema is empty; Desktop page shows no fake editable setting or Save. Real Electron smoke and screenshot `output/playwright/p4-04-plugin-desktop.png` confirm plugin state and Renderer isolation. Frozen install, contracts 47/0 error/4 old warnings, task map 37 deferred, Python 110 pytest/Ruff/mypy 44 files, lint/typecheck/test/build/Desktop smoke/diff all exit 0. T106–T110 retain original references and are deferred to P6. No new dependency, SQL, credentials or release. See ADR 0051. P4-05 starts next.
P4-03: Bundled `plugins.lock.json` fixes exact Codex manifest/entry/config bytes and canonical package digest, checked at discovery/activation/new Run. Production RunConfig adds plugin ID/version/contentHash alongside the prior Executor/upstream lock without rewriting old snapshots or SQLite. Registry Run lease prevents adapter replacement; deactivation enters draining and blocks new starts until final release. An actual Electron→Python Host→Codex Run `73dcf502-5737-4684-bf86-a3c9fcf1c4d1` stored the exact lock in SQLite and completed Verify/Review/human acceptance/local merge; source Git clean. `pnpm test:p3-live` exit 0 after a first failure in an unregistered synthetic Executor fixture, corrected by limiting Registry enforcement to Registry-owned production adapters and rerunning all six scenarios plus live chain. Wheel/sdist include lock and plugin files. Frozen install, contracts/task map (32 deferred), Python 108 pytest/Ruff/strict mypy 44 source files, lint/typecheck/test/build/Desktop smoke/schema24 and diff check all exit 0. No dependency or DB migration. See ADR 0050. P4-04 starts next.
P4-02: Registry now topologically resolves Host service dependencies before import, rejects duplicate/missing/cyclic services, stages Executor contribution and gives every activation a reverse-order DisposableScope. Failed activation invokes plugin cleanup and withdraws all staged resources; normal deactivation unregisters first. T036: ten synthetic listener/resource cycles plus ten real owned Python child processes exit before next activation. T037: injected partial failure leaves no listener/Executor and Host-owned process service remains usable. Final frozen install, contracts (47/0 error/4 existing warnings), task map (35 deferred before verified T036/T037 mappings were removed; 33 afterward), Python check (105 pytest/Ruff/strict mypy 43 source files), lint/typecheck/test/build, actual Electron Desktop smoke/schema24, and diff check all exit 0. No dependency/SQLite/Renderer change. See ADR 0049. P4-03 starts next.
P4-01: Pure Python Manifest preflight parses exact/caret API ranges and verifies allowlisted source, bounded files/symlinks, platform, permission, services and a closed config schema before entry import. Invalid cases yield field-specific error lists, leave Registry/Executor untouched and an import sentinel confirms no entry side effect. `pnpm py:check` passed 98 pytest/Ruff/strict mypy 42 source files; frozen install, contracts (47/0 error/4 existing warnings), task map (33 deferred), lint/typecheck/test/build, real Electron Desktop smoke/schema24, `pnpm probe:codex` (local Node-era compatibility probe, not second Python Executor acceptance), and diff check all exit 0. No dependency/SQLite/Renderer change. See ADR 0048. P4-02 starts next.
Baseline: MIG-PY-01 frozen install, contract/task-map validation, lint, typecheck, test, build, real Electron Desktop smoke and git diff check all passed on macOS arm64 after two minimal P2 partial-edit lint fixes. Logs: `/tmp/forge-mig-py-01-*.log`. No database reset, cleanup, commit or push. Existing uncommitted changes preserved.
MIG-PY-02～05 historical evidence: Python 3.12.13, uv 0.11.14, frozen lock and documented licenses; independent stdio Host, versioned bounded JSON-RPC; frozen Node migrations v1–15/checksums, isolated additive v16, rollback/durability and Node↔Python Project/Task/Run SQLite parity; Python P1 services passed isolated offline and authenticated online Codex draft→approval→TODO→restart. The earlier read-only dual-Host condition ended at MIG-PY-09. See ADR 0030/0031.
MIG-PY-06: Python worktree ownership/lease/epoch, process-tree cancellation, RunConfig/Run/Context/Checkpoint, provider-neutral event gate and bounded scheduler, redacted observations/real Diff, immutable Git CodeSnapshot and SQLite Handoff, plus a one-node development entry are implemented. Real disposable Git/SQLite/subprocess tests verify success/failure/cancel/shutdown/unknown event isolation, source cleanliness and restart; a separate Python Host stdio process reads Run/inspection and rejects production start without an Executor. `pnpm py:check` passes 29 tests, Ruff and strict mypy (22 source files). Frozen install, contracts/task map, lint, typecheck, test, build, Node↔Python DB parity, dual-Host Desktop smoke and `git diff --check` pass. See ADR 0032. The core fixture checks are MIG06 DONE; true Codex T041–T045 belongs MIG07, Python-only Desktop P2-01～09 acceptance MIG09. All remain DEFERRED_VERIFICATION, not PASSED. P2-10 stays paused; P3 is untouched.
MIG-PY-07: Python Codex 0.155.1 app-server over owned stdio, strict bounded JSON-RPC, real model/auth probe, standard event mapping, explicit approval, cancel, continuation and process cleanup are implemented. `pnpm test:python-codex-live` passed missing-auth/invalid-model/structured/coding/cancel/approve/reject/continuation/restart-continuation and independent Python Host P1→Codex Run→Handoff on disposable Git/SQLite; source remained unchanged. `pnpm py:check` passed 33 tests, Ruff and strict mypy on 24 source files. Frozen pnpm install, contract/task-map validation, lint/typecheck/test/build (test and smoke include build), dual-Host Desktop smoke, wheel/sdist packaging and `git diff --check` passed. See ADR 0033. Historical Node Desktop remains temporary writer until MIG09; canonical T041 dual-adapter, T042 Profile-save and T045 mid-run auth expiry retain their downstream deferred owners. P2-10 stays paused; P3 untouched.
MIG-PY-08: Python trusted built-in PluginRegistry validates the authoritative manifest fields before code import, enforces API/platform/service/permission/contribution gates, stages registrations and disposes in reverse; only `forge.executor.codex` is registered. Host/Core resolve the provider-neutral Executor Adapter through the registry rather than directly importing Codex. See ADR 0034. Third-party isolation and second adapter remain P4.
MIG-PY-09: Electron Main now imports/starts only the owned writable Python Host. Fixed Renderer commands use validated JSON-RPC stdio; Python applies additive SQLite schema 16 and solely owns business writes. Real Electron smokes passed Python health/degraded/crashed/cleanup, P1 project trust→manual Draft→human approval TODO→restart, and authenticated Codex Run→Diff/CodeSnapshot/Handoff through Renderer bridge with clean source Git. Python ProcessController live cancel passed. `pnpm py:check` passed 37 pytest, Ruff and strict mypy; frozen install, contract/task-map, lint/typecheck/test/build/smoke and diff check passed. `pnpm dev:desktop` Ctrl+C removed its Python Host/Vite/Electron PIDs. Current user development DB file was absent; existing user-data v15→16, installer, Windows x64 and macOS Intel remain UNVERIFIED. See ADR 0035 and implementation status. Migration ended before P2-10 resumed.
P2-10: Real Electron Renderer→Main→Python Host→Codex completion `e67668ee-a32f-4718-9831-043ac3919746` changed isolated `math.js`/`test.js`, passed fixture test and published CodeSnapshot `1590ba02-38f1-4bea-b8b1-218ba171c290` with `unverified` formal AC. Provider fault Run `97b6b00b-8598-4cb3-9952-34871ee40bff` failed with `run.failed` and no Handoff; separate Desktop cancellation Run `07698a53-3f18-464b-8b7e-7a5ee6dd57bc` reached `cancelled`, stopped its owned process and did not continue writing. Manual diff/screenshot checks and full quality chain pass; see `docs/demo/p2-python-vertical.md` and P2 report. An earlier no-change provider completion was rejected as successful evidence. P2 Phase Gate passed for current development platform; T116–T120 remain deferred, and P3-01 starts next.
P3-01: Python Host owns `ReviewCopyManager`, which validates a frozen CodeSnapshot Git ref/tree, creates a detached review worktree, records ownership, rejects unavailable/non-native read-only capability and checks exact read-only/never-approval request identity. Real Git test confirms fixed copy after source branch advances, tracked/untracked content, refusal to delete modified/active/foreign copy and symlink defense. Live Codex 0.155.1 diagnostic in the copy produced `approval.requested` for a write, explicit rejection and clean copy/source; production request remains `read-only + never`. `pnpm py:check` has 38 tests, Ruff and strict mypy 29 source files; frozen install, contracts/task-map (37 deferred), lint/typecheck/test/build/Desktop smoke and diff check passed. T050 old-base merge/revalidation remains deferred to P3-08. See ADR 0036. P3-02 starts next.
Last updated: 2026-09-24

P3-12: Six isolated Python/Git/SQLite acceptance scenarios and real `pnpm test:p3-live` passed. Electron→Python Host→Codex Run `bf363d89-995f-42e5-8ded-2d35a45ae74b` produced Verify `5873406e-f1b5-40e9-ba69-88dcfc33098f` (exit 0), CodeSnapshot `9cbd3e8b-e85b-4f3a-a56d-1a7fd4076781`, approved Review `b1b0ec13-0304-4ce6-a07f-6482c875c0db`, separate human acceptance `27b67f24-4fb0-499c-8a8a-960bc696f577`, then separately confirmed local merge `ae82b9ae-c949-44dc-b693-df05d1caf951`; source Git clean. A second actual Electron/Host mixed-status board probe rejected cross-column drag and verified filters and keyboard reorder. Five real app screenshots were visually inspected. Final frozen install, contracts (47 files, 0 errors, 4 existing warnings), task map (29 deferred), Python check (83 pytest/Ruff/strict mypy), lint/typecheck/test/build, Electron Desktop smoke/schema24 and `git diff --check` all exit 0. T116–T120 remain DEFERRED_VERIFICATION to P6/P9; Windows/Intel/installer/user-DB upgrade remain UNVERIFIED. See ADR 0047 and P3 report. P3 Phase Gate passed; P4-01 starts next.

P3-11: Python Host now uses SQLite's online backup API for committed WAL state, with a private atomic backup and quick/FK checks. Existing-schema upgrade does a free-space preflight and backup; an injected broken migration rolls back, preserves old rows and latches read-only diagnostics. A real SQLite FULL condition rolls back and reports unavailable without a success marker. Additive schema24 holds bounded immutable imported text/JSON verifier artifacts and namespace-scoped plugin JSON. Independent process death during Task/approval/event transaction leaves no half commit; independent Host upgrades v23→24 preserving approved Task and v23 backup. T086–T090 interface-level evidence and T057 import path/MIME guard were exercised; malicious third-party Python plugin OS isolation and external Git objects in a DB backup are not claimed. `pnpm py:check` passed 82 pytest/Ruff/strict mypy, frozen install/contracts/task map (28 deferred), TS quality/build and Electron Desktop smoke/schema24 passed. Existing user DB, installer, Windows/Intel remain unverified. See ADR 0046 and implementation status. P3-12 starts next.

P3-10: Python Host owns an explicit approved Task revision proposal, separate exact-hash human decision and safe-point apply in SQLite schema23. Old RunConfig/Run and source Git stay frozen; active Run/Review/Verify/rework or interrupted state delays application, and a pending approved change blocks a new run or review. Applying the new revision makes previous snapshot-based current AC/final acceptance/Done stale while preserving historical records and source links. Four Python tests include a real independent stdio Host restart, scope/rejection, active Run and old Review/Verify denial; Vue tests check approval separation, pending state and historical Handoff warning. Frozen install/contracts/task map (29 deferred), 73 Python pytest/Ruff/mypy, lint/typecheck/test/build and real Electron Desktop smoke/schema23 passed after updating a stale smoke assertion; diff check passed. Current user DB upgrade, Windows/Intel/installer and live Codex mid-turn change remain unverified. See ADR 0045 and implementation status. P3-11 starts next.

P3-09: Python Host first writable handshake now fences unfinished prior-runtime Runs as interrupted, quarantines active leases and writes a reasoned audit event; historical PIDs/session refs cannot establish safe ownership for automatic kill or resume. Startup audits all durable merge intents without Git writes, completing only an exact two-parent target result backed by the Forge-owned candidate; base-unchanged intent requires human, and missing candidate/divergent outcome is unknown. Independent Python Host processes were killed at three real Git/SQLite boundaries on disposable fixtures; a fourth case removed candidate evidence. Restart never replayed merge. Run duplicate/old epoch, finite no-side-effect 429 and owned process-tree cancellation tests pass. Frozen install/contracts/task map, 69 Python pytest/Ruff/mypy, lint/typecheck/test/build, real Electron Desktop smoke/schema22 and diff check pass; UI unresolved-intent test was rerun. Three T034 deferred references removed. Windows/Intel/installer, current user DB and safe orphan process reattachment remain unverified. See ADR 0044 and implementation status. P3-10 starts next.

P3-08: Python Host freezes an immutable accepted DeliverySummary in additive SQLite schema22 and reserves a unique merge intent before changing Git. Target branch must be the clean checked-out local branch at the exact validated base HEAD; moved target is rejected for revalidation. Forge-owned candidate worktree produces a two-parent commit, then `--ff-only` updates the local target, with no force/push/deploy. Same-key retry and restart-after-Git reconciliation return one result or explicit unknown. Electron Main shows a native confirmation after strict source/schema validation, and the Vue panel requires separate deliberate confirmation. Four real Git/SQLite/Host JSON-RPC tests plus authenticated Electron→Python Host→Codex→Verify→Review→Owner acceptance→local merge passed; native dialog cancel created no operation. Result commit `ef9e229a8ac85b9087c3ec69b9e000dcd29defbf`; actual screenshot `output/playwright/p3-08-python-desktop-merged-1440x900.png` inspected. First live script had an undefined test field; second exposed thread-bound SQLite misuse; both were fixed and the third full live run passed. Frozen install/contracts/task map (32 deferred), 63 Python pytest/Ruff/mypy, lint/typecheck/test/build/Desktop smoke/schema22/diff passed after a TS lint correction. T050/T060 removed from deferred, T057 remains P3-11. Existing user DB upgrade, Windows/Intel/installer and hard Host crash orphan cleanup unverified. See ADR 0043 and implementation status. P3-09 starts next.

P3-07: Python Host owns current Contract/CodeSnapshot/Review/Verify/AC evidence-basis CAS and immutable final Owner decisions in SQLite v21. Explicit accept projects Board Done without merge, push or deploy; return starts a new bounded Attempt from the pinned snapshot, with new evidence required. Non-security advisory waiver stores exact issue/review/snapshot/revision, Host Owner actor and reason; blocking issues cannot be waived. Real Git/SQLite/Host and Vue tests prove stale rejection, issue waiver and return. Authenticated Electron→Python Host→Codex→Verify→Review→human accept completed on a disposable fixture (decision `08100199-df15-401a-8f6c-59c53448ac63`), Board Done and source Git clean; screenshot `output/playwright/p3-07-python-desktop-accepted-1440x900.png` inspected. Frozen install, contracts/task map (35 deferred), 58 Python pytest/Ruff/mypy, lint/typecheck/test/build/Desktop smoke and diff check passed. T055/T059 have evidence, T034 remains deferred to P3-09. Existing user DB migration, Windows/Intel/installer and live provider advisory occurrence unverified. See ADR 0042 and implementation status. P3-08 starts next.

P3-05: Python Host now projects every approved Contract AC against the current CodeSnapshot. Additive SQLite schema 19 stores immutable, idempotent per-criterion decisions. A passed command alone leaves `inconclusive/unverified`; explicit current-snapshot report mapping and reason can mark an automated AC `verified`, while human risk remains distinct and final Owner acceptance still required. Real Git/SQLite/independent Host tests, 49 Python pytest, Ruff/mypy, contract/task-map, lint/typecheck/test/build/Desktop smoke/diff and real Electron→Python Host→Codex Vue interaction passed. Live development Run `f84ff677-9220-4039-903e-02dcbb673e64`, Verify `ff5acda7-4576-4b6e-84db-ccaa71b2db9b`, fixed snapshot `2ecc754a-26be-4cb4-b8ed-048596e06f19`; actual screenshot inspected at `output/playwright/p3-05-python-desktop-acceptance-1440x900.png`, source Git clean. T044 exercised; 39 deferred cases remain. Existing user DB migration, Windows/Intel/installer/hard Host crash unverified. See ADR 0040. P3-06 started.

P3-06: Python Host owns durable shared Review/Verify rework cycles in additive SQLite schema 20. Failed immutable report reserves one cycle per latest source snapshot, starts a new Developer Run/Attempt from its pinned commit with bounded provenance feedback, then reruns the same failed gate after new Handoff. Three cycles/twenty total attempts are enforced; exceeded limits project blocked without creating a new Run or marking Task Done. A real disposable Git/SQLite/owned-process fixture exercised Verify fail→repair→pass, Review blocker→new Attempt, mixed route counting, ceiling and mid-rework cancellation; source stayed clean. P3-06 tests, contracts, UI, frozen install, lint/typecheck/test/build, real Desktop smoke/schema20, and authenticated Electron→Python Host→Codex development/Verify regression passed. Live Codex forced Review blocker loop and hard Host crash recovery were not exercised; T034 remains deferred to P3-09. See ADR 0041 and implementation status. P3-07 has started.

P3-02: Python Reviewer Profile/prompt and independent bounded context are packaged in the wheel; `ReviewResult` rejects missing, invalid, stale, unanchored or unsupported approval. Real Codex 0.155.1 structured output on the pinned read-only copy returned `inconclusive` for missing full Diff; no false approval. `pnpm py:check` passed 40 pytest, Ruff and strict mypy 30 files. Frozen install, contracts/task map (39 deferred), lint/typecheck/test/build/Desktop smoke and diff check passed. T054/T055 remain deferred, not passed. See ADR 0037. P3-03 starts next.

P3-03: Python Host gained explicit snapshot-bound read-only Reviewer jobs, immutable Review reports, stable Issue threads and per-attempt occurrences, compact ReworkHandoff, and additive SQLite schema 17. Real Git/SQLite test verified dedup across attempts/snapshots, stale old results and independent stdio reads. Real Electron→Python Host→Codex developed a fixture then ran Review `de458133-4ef7-4639-a9f5-0c9422a3460f`; structured result `approved` for fixed snapshot `578ce16a-5f91-4249-9d1a-5a1ff3a1eac6`, source Git clean, screenshot checked. Frozen install, contracts, task map (38 deferred), 41 Python pytest/Ruff/mypy, TS lint/typecheck/test/build, Desktop smoke and diff check passed after correcting one stale schema-16 smoke expectation. T054 verified by fixture plus live protocol; T055 remains deferred. See ADR 0038. P3-04 begins next.

P3-04: Python Host now executes only approved CommandPreset argv/cwd against a detached copy of the latest immutable CodeSnapshot, with ProcessController timeout/owned-tree cleanup, bounded redacted stdout/stderr artifacts, exit-code authority, idempotent jobs and additive SQLite schema 18. Real Git/SQLite/subprocess tests covered pass, false PASS with exit 7, timeout, no-test `not_configured`, source isolation, immutable report, restart and independent Host stdio. Real Electron→Main→Python Host→Codex development snapshot `ca377444-d54a-4b09-92cf-4b5c07f2a593` then approved `node test.js` returned Verify `295339f0-1ef1-4242-b06e-8626419a2924` passed/exit 0; source clean. Frozen install, contracts/task map (41 deferred), Python 46 pytest/Ruff/mypy 33 source files, TS lint/typecheck/test/build, Desktop smoke/schema18, wheel build and diff check passed after correcting a stale schema-17 smoke assertion. T056/T058 exercised; T057/T059/T060 remain deferred to P3-11/P3-07/P3-08. See ADR 0039. P3-05 starts next.
