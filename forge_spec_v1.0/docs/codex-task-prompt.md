# 单任务实施提示词

你正在实现 Forge。先阅读根目录 AGENTS.md、START_HERE.md 与主蓝图。当前任务是：**P0-01**（每次由用户替换成一个taskId）。

请先检查仓库实际状态和该任务依赖，明确对应模块、公开契约、页面和T编号验收用例。给出本任务的最小实现范围，然后实际修改代码、补齐测试并运行验证。使用已经冻结的架构，不新增未经要求的框架或服务。

要求：
- 本次不越过阶段边界；不得以Mock/硬编码运行记录代替真实SDK、执行、审批或状态流转。
- 占位界面允许仅在明确Demo模式；正式完成必须有实际Command与Query支撑。
- 上游SDK接口必须查官方文档并按锁定版本探测；遇到凭据、签名、OS无法测试的情况明确阻塞，不虚构结果。
- 验收条件与安全门禁不可降低；发现契约冲突，先报告并给出ADR，不自行静默改变。
- 完成后说明实现文件、实际测试命令和结果、证据路径、未验证事项与下一任务。

不要一次生成整个产品。先把当前任务做成可运行、可测量、可复查的增量。
